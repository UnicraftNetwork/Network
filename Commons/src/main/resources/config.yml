onjoin-tools: true
navigator:
  enabled: true
  title: "&b&lAvailable Servers"
  size: 27
  servers:
    13:
      name: Local
      ip: localhost
      port: 25565
      item:
        material: GRASS_BLOCK
        title: "&b&lElMedievo"
        lore:
          - ""
          - "&aOnline: %player_count%"
chat:
  vault-formatting:
    enabled: true
    format: "<{prefix}{name}{suffix}> {message}"
force-gamemode:
  enabled: true
  exempt-permission: "commons.defaultgamemode.exempt"
  gamemode: survival
weather:
  enabled: true